# Runtime Infrastructure

This node owns routing for generic infrastructure runtime only. Its currently
approved leaf is:

- QuestDB Runtime → [questdb-runtime.md](questdb-runtime.md)

No other runtime, service-manager, recorder-runtime, deployment, or supervisor
hierarchy is defined here.
